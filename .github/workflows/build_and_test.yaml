name: 'Build And Test'
on:
  workflow_call: 
jobs: 
  main:
    name: 'Build And Test'
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v4
      - uses: ./.github/workflows/setup_dotnet.yaml

      - name: Build
        run: dotnet build -t:Rebuild --no-restore
        
      - name: Unit Tests
        run: dotnet test --no-build --no-restore
